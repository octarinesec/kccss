name: "seccomp"
type: "remediation"
id: "R-1"
revision: 1
title: "Workload containers have seccomp policy"
shortDescription: ""
description: "A seccomp policy specify which system class can be called by the application. It is a sandboxing technique that reduces the chance that a kernel vulnerability will be successfully exploited"
confidentiality: 
  impact: "High"
  description: "A seccomp policy can prevent malicious programs from reading files not used by the container"
integrity:
  impact: "High"
  description: "A seccomp policy can prevent malicious programs to use kernel exploits to break out of the container"
availability: 
  impact: "High"
  description: "A seccomp policy can be used to restrict the system calls and prevent processes from grabbing additional CPU or memory resources"
exploitability: "None"
attackVector: "Local"
scope: "Host"